/**
 * Validate Command
 *
 * codesquad validate — Run static checks and coverage reports.
 * Phase 6.3: CLI entry point for the validate system.
 */
export declare function handleValidate(subcommand: string, options: {
    all?: boolean;
    name?: string;
    failOnWarn?: boolean;
    strict?: boolean;
}): Promise<void>;
//# sourceMappingURL=validate.d.ts.map