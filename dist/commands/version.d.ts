/**
 * Version Command
 *
 * codesquad version — Display version info and check for updates.
 * Phase 7.1: CLI entry point for version checks.
 */
export declare function handleVersion(options: {
    check?: boolean;
    json?: boolean;
}): Promise<void>;
//# sourceMappingURL=version.d.ts.map